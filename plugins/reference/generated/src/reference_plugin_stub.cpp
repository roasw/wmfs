// Generated by wmfs-tool. Implement the declared operation functions.
#include <wmfs/reference_plugin.hpp>

namespace {
int32_t dispatch(const wmfs_invocation_v1 *invocation) {
    if (invocation == 0 ||
        invocation->struct_size < sizeof(wmfs_invocation_v1)) {
        return WMFS_STATUS_INVALID_ARGUMENT;
    }
    switch (invocation->operation_id) {
    case UINT32_C(1):
        if (invocation->input_count != UINT32_C(2) ||
            invocation->output_count != UINT32_C(1) ||
            invocation->scalar_count != UINT32_C(0)) {
            return WMFS_STATUS_INVALID_ARGUMENT;
        }
        if ((invocation->input_count && invocation->inputs == 0) ||
            (invocation->output_count && invocation->outputs == 0) ||
            (invocation->scalar_count && invocation->scalars == 0))
            return WMFS_STATUS_INVALID_ARGUMENT;
        if (invocation->inputs[0].dtype != WMFS_DTYPE_FLOAT32 &&
            invocation->inputs[0].dtype != WMFS_DTYPE_FLOAT64 &&
            invocation->inputs[0].dtype != WMFS_DTYPE_INT64 &&
            invocation->inputs[0].dtype != WMFS_DTYPE_UINT8)
            return WMFS_STATUS_UNSUPPORTED;
        if (invocation->inputs[1].dtype != WMFS_DTYPE_FLOAT32 &&
            invocation->inputs[1].dtype != WMFS_DTYPE_FLOAT64 &&
            invocation->inputs[1].dtype != WMFS_DTYPE_INT64 &&
            invocation->inputs[1].dtype != WMFS_DTYPE_UINT8)
            return WMFS_STATUS_UNSUPPORTED;
        if (invocation->inputs[1].dtype != invocation->inputs[0].dtype)
            return WMFS_STATUS_INVALID_ARGUMENT;
        if (invocation->outputs[0].dtype != invocation->inputs[1].dtype)
            return WMFS_STATUS_INVALID_ARGUMENT;
        switch (invocation->inputs[0].dtype) {
        case WMFS_DTYPE_FLOAT32:
            return wmfs::reference::matmul_typed(
                wmfs::reference::dtype_tag<float>(), invocation);
        case WMFS_DTYPE_FLOAT64:
            return wmfs::reference::matmul_typed(
                wmfs::reference::dtype_tag<double>(), invocation);
        case WMFS_DTYPE_INT64:
            return wmfs::reference::matmul_typed(
                wmfs::reference::dtype_tag<std::int64_t>(), invocation);
        case WMFS_DTYPE_UINT8:
            return wmfs::reference::matmul_typed(
                wmfs::reference::dtype_tag<std::uint8_t>(), invocation);
        default:
            return WMFS_STATUS_UNSUPPORTED;
        }
    case UINT32_C(2):
        if (invocation->input_count != UINT32_C(1) ||
            invocation->output_count != UINT32_C(3) ||
            invocation->scalar_count != UINT32_C(1)) {
            return WMFS_STATUS_INVALID_ARGUMENT;
        }
        if ((invocation->input_count && invocation->inputs == 0) ||
            (invocation->output_count && invocation->outputs == 0) ||
            (invocation->scalar_count && invocation->scalars == 0))
            return WMFS_STATUS_INVALID_ARGUMENT;
        if (invocation->inputs[0].dtype != WMFS_DTYPE_FLOAT32 &&
            invocation->inputs[0].dtype != WMFS_DTYPE_FLOAT64)
            return WMFS_STATUS_UNSUPPORTED;
        if (invocation->outputs[0].dtype != invocation->inputs[0].dtype)
            return WMFS_STATUS_INVALID_ARGUMENT;
        if (invocation->outputs[1].dtype != invocation->inputs[0].dtype)
            return WMFS_STATUS_INVALID_ARGUMENT;
        if (invocation->outputs[2].dtype != invocation->inputs[0].dtype)
            return WMFS_STATUS_INVALID_ARGUMENT;
        switch (invocation->inputs[0].dtype) {
        case WMFS_DTYPE_FLOAT32:
            return wmfs::reference::svd_typed(
                wmfs::reference::dtype_tag<float>(), invocation);
        case WMFS_DTYPE_FLOAT64:
            return wmfs::reference::svd_typed(
                wmfs::reference::dtype_tag<double>(), invocation);
        default:
            return WMFS_STATUS_UNSUPPORTED;
        }
    case UINT32_C(3):
        if (invocation->input_count != UINT32_C(1) ||
            invocation->output_count != UINT32_C(1) ||
            invocation->scalar_count != UINT32_C(1)) {
            return WMFS_STATUS_INVALID_ARGUMENT;
        }
        if ((invocation->input_count && invocation->inputs == 0) ||
            (invocation->output_count && invocation->outputs == 0) ||
            (invocation->scalar_count && invocation->scalars == 0))
            return WMFS_STATUS_INVALID_ARGUMENT;
        if (invocation->inputs[0].dtype != WMFS_DTYPE_FLOAT32 &&
            invocation->inputs[0].dtype != WMFS_DTYPE_FLOAT64 &&
            invocation->inputs[0].dtype != WMFS_DTYPE_INT64 &&
            invocation->inputs[0].dtype != WMFS_DTYPE_UINT8)
            return WMFS_STATUS_UNSUPPORTED;
        switch (invocation->inputs[0].dtype) {
        case WMFS_DTYPE_FLOAT32:
            return wmfs::reference::add_scalar_typed(
                wmfs::reference::dtype_tag<float>(), invocation);
        case WMFS_DTYPE_FLOAT64:
            return wmfs::reference::add_scalar_typed(
                wmfs::reference::dtype_tag<double>(), invocation);
        case WMFS_DTYPE_INT64:
            return wmfs::reference::add_scalar_typed(
                wmfs::reference::dtype_tag<std::int64_t>(), invocation);
        case WMFS_DTYPE_UINT8:
            return wmfs::reference::add_scalar_typed(
                wmfs::reference::dtype_tag<std::uint8_t>(), invocation);
        default:
            return WMFS_STATUS_UNSUPPORTED;
        }
    case UINT32_C(4):
        if (invocation->input_count != UINT32_C(3) ||
            invocation->output_count != UINT32_C(2) ||
            invocation->scalar_count != UINT32_C(0)) {
            return WMFS_STATUS_INVALID_ARGUMENT;
        }
        if ((invocation->input_count && invocation->inputs == 0) ||
            (invocation->output_count && invocation->outputs == 0) ||
            (invocation->scalar_count && invocation->scalars == 0))
            return WMFS_STATUS_INVALID_ARGUMENT;
        if (invocation->inputs[0].dtype != WMFS_DTYPE_FLOAT32 &&
            invocation->inputs[0].dtype != WMFS_DTYPE_FLOAT64 &&
            invocation->inputs[0].dtype != WMFS_DTYPE_INT64 &&
            invocation->inputs[0].dtype != WMFS_DTYPE_UINT8)
            return WMFS_STATUS_UNSUPPORTED;
        if (invocation->inputs[1].dtype != WMFS_DTYPE_FLOAT32 &&
            invocation->inputs[1].dtype != WMFS_DTYPE_FLOAT64 &&
            invocation->inputs[1].dtype != WMFS_DTYPE_INT64 &&
            invocation->inputs[1].dtype != WMFS_DTYPE_UINT8)
            return WMFS_STATUS_UNSUPPORTED;
        if (invocation->inputs[1].dtype != invocation->inputs[0].dtype)
            return WMFS_STATUS_INVALID_ARGUMENT;
        if (invocation->inputs[2].dtype != WMFS_DTYPE_FLOAT32 &&
            invocation->inputs[2].dtype != WMFS_DTYPE_FLOAT64 &&
            invocation->inputs[2].dtype != WMFS_DTYPE_INT64 &&
            invocation->inputs[2].dtype != WMFS_DTYPE_UINT8)
            return WMFS_STATUS_UNSUPPORTED;
        if (invocation->inputs[2].dtype != invocation->inputs[0].dtype)
            return WMFS_STATUS_INVALID_ARGUMENT;
        if (invocation->outputs[0].dtype != invocation->inputs[2].dtype)
            return WMFS_STATUS_INVALID_ARGUMENT;
        if (invocation->outputs[1].dtype != invocation->inputs[2].dtype)
            return WMFS_STATUS_INVALID_ARGUMENT;
        switch (invocation->inputs[0].dtype) {
        case WMFS_DTYPE_FLOAT32:
            return wmfs::reference::matmul_vjp_typed(
                wmfs::reference::dtype_tag<float>(), invocation);
        case WMFS_DTYPE_FLOAT64:
            return wmfs::reference::matmul_vjp_typed(
                wmfs::reference::dtype_tag<double>(), invocation);
        case WMFS_DTYPE_INT64:
            return wmfs::reference::matmul_vjp_typed(
                wmfs::reference::dtype_tag<std::int64_t>(), invocation);
        case WMFS_DTYPE_UINT8:
            return wmfs::reference::matmul_vjp_typed(
                wmfs::reference::dtype_tag<std::uint8_t>(), invocation);
        default:
            return WMFS_STATUS_UNSUPPORTED;
        }
    case UINT32_C(5):
        if (invocation->input_count != UINT32_C(1) ||
            invocation->output_count != UINT32_C(1) ||
            invocation->scalar_count != UINT32_C(0)) {
            return WMFS_STATUS_INVALID_ARGUMENT;
        }
        if ((invocation->input_count && invocation->inputs == 0) ||
            (invocation->output_count && invocation->outputs == 0) ||
            (invocation->scalar_count && invocation->scalars == 0))
            return WMFS_STATUS_INVALID_ARGUMENT;
        if (invocation->inputs[0].dtype != WMFS_DTYPE_FLOAT32 &&
            invocation->inputs[0].dtype != WMFS_DTYPE_FLOAT64 &&
            invocation->inputs[0].dtype != WMFS_DTYPE_INT64 &&
            invocation->inputs[0].dtype != WMFS_DTYPE_UINT8)
            return WMFS_STATUS_UNSUPPORTED;
        if (invocation->outputs[0].dtype != invocation->inputs[0].dtype)
            return WMFS_STATUS_INVALID_ARGUMENT;
        switch (invocation->inputs[0].dtype) {
        case WMFS_DTYPE_FLOAT32:
            return wmfs::reference::add_scalar_vjp_typed(
                wmfs::reference::dtype_tag<float>(), invocation);
        case WMFS_DTYPE_FLOAT64:
            return wmfs::reference::add_scalar_vjp_typed(
                wmfs::reference::dtype_tag<double>(), invocation);
        case WMFS_DTYPE_INT64:
            return wmfs::reference::add_scalar_vjp_typed(
                wmfs::reference::dtype_tag<std::int64_t>(), invocation);
        case WMFS_DTYPE_UINT8:
            return wmfs::reference::add_scalar_vjp_typed(
                wmfs::reference::dtype_tag<std::uint8_t>(), invocation);
        default:
            return WMFS_STATUS_UNSUPPORTED;
        }
    case UINT32_C(6):
        if (invocation->input_count != UINT32_C(1) ||
            invocation->output_count != UINT32_C(1) ||
            invocation->scalar_count != UINT32_C(1)) {
            return WMFS_STATUS_INVALID_ARGUMENT;
        }
        if ((invocation->input_count && invocation->inputs == 0) ||
            (invocation->output_count && invocation->outputs == 0) ||
            (invocation->scalar_count && invocation->scalars == 0))
            return WMFS_STATUS_INVALID_ARGUMENT;
        if (invocation->inputs[0].dtype != WMFS_DTYPE_FLOAT32 &&
            invocation->inputs[0].dtype != WMFS_DTYPE_FLOAT64 &&
            invocation->inputs[0].dtype != WMFS_DTYPE_INT64 &&
            invocation->inputs[0].dtype != WMFS_DTYPE_UINT8)
            return WMFS_STATUS_UNSUPPORTED;
        if (invocation->scalars[0].kind != WMFS_SCALAR_INT64 ||
            invocation->scalars[0].bits >= UINT64_C(2))
            return WMFS_STATUS_INVALID_ARGUMENT;
        switch (invocation->inputs[0].dtype) {
        case WMFS_DTYPE_FLOAT32:
            return wmfs::reference::nonzero_typed(
                wmfs::reference::dtype_tag<float>(), invocation);
        case WMFS_DTYPE_FLOAT64:
            return wmfs::reference::nonzero_typed(
                wmfs::reference::dtype_tag<double>(), invocation);
        case WMFS_DTYPE_INT64:
            return wmfs::reference::nonzero_typed(
                wmfs::reference::dtype_tag<std::int64_t>(), invocation);
        case WMFS_DTYPE_UINT8:
            return wmfs::reference::nonzero_typed(
                wmfs::reference::dtype_tag<std::uint8_t>(), invocation);
        default:
            return WMFS_STATUS_UNSUPPORTED;
        }
    default:
        return WMFS_STATUS_UNSUPPORTED;
    }
}

int32_t plan_outputs(const wmfs_invocation_v1 *invocation,
                     wmfs_output_plan_v1 *outputs, uint32_t output_capacity,
                     uint32_t *output_count) {
    if (invocation == 0 || outputs == 0 || output_count == 0 ||
        invocation->struct_size < sizeof(wmfs_invocation_v1)) {
        return WMFS_STATUS_INVALID_ARGUMENT;
    }
    switch (invocation->operation_id) {
    case UINT32_C(6):
        if (invocation->input_count != UINT32_C(1) ||
            invocation->output_count != UINT32_C(0) ||
            invocation->scalar_count != UINT32_C(1) ||
            (invocation->input_count && invocation->inputs == 0) ||
            (invocation->scalar_count && invocation->scalars == 0)) {
            return WMFS_STATUS_INVALID_ARGUMENT;
        }
        switch (invocation->inputs[0].dtype) {
        case WMFS_DTYPE_FLOAT32:
            return wmfs::reference::nonzero_plan_typed(
                wmfs::reference::dtype_tag<float>(), invocation, outputs,
                output_capacity, output_count);
        case WMFS_DTYPE_FLOAT64:
            return wmfs::reference::nonzero_plan_typed(
                wmfs::reference::dtype_tag<double>(), invocation, outputs,
                output_capacity, output_count);
        case WMFS_DTYPE_INT64:
            return wmfs::reference::nonzero_plan_typed(
                wmfs::reference::dtype_tag<std::int64_t>(), invocation, outputs,
                output_capacity, output_count);
        case WMFS_DTYPE_UINT8:
            return wmfs::reference::nonzero_plan_typed(
                wmfs::reference::dtype_tag<std::uint8_t>(), invocation, outputs,
                output_capacity, output_count);
        default:
            return WMFS_STATUS_UNSUPPORTED;
        }
    default:
        return WMFS_STATUS_UNSUPPORTED;
    }
}

const wmfs_plugin_api_v1 API = {sizeof(wmfs_plugin_api_v1),
                                WMFS_REFERENCE_ABI_VERSION,
                                WMFS_REFERENCE_PROTOCOL_VERSION,
                                UINT32_C(0),
                                "reference",
                                "0.1.0",
                                WMFS_REFERENCE_INTERFACE_FINGERPRINT,
                                &dispatch,
                                &plan_outputs};
} // namespace

extern "C" const wmfs_plugin_api_v1 *
wmfs_reference_plugin_get_api(uint32_t abi_version) {
    return abi_version == WMFS_REFERENCE_ABI_VERSION ? &API : 0;
}
